/**
 * The app's icon vocabulary, mapped onto lucide-react.
 *
 * One library, one stroke weight, one visual language. The `Icon*` aliases keep
 * call sites stable and make the app's semantic set explicit: a page asks for
 * "memories", not for whichever glyph happens to look right.
 */
export {
  LayoutDashboard as IconHome,
  Hash as IconHash,
  BookOpen as IconBook,
  Search as IconSearch,
  MessageSquare as IconChat,
  Library as IconMemory,
  Boxes as IconSpaces,
  Plug as IconPlug,
  Settings as IconSettings,
  Plus as IconPlus,
  ArrowRight as IconArrowRight,
  ExternalLink as IconExternal,
  Trash2 as IconTrash,
  LogOut as IconLogout,
  Sparkles as IconSparkle,
  Clock as IconClock,
  BarChart3 as IconLayers,
  // Newly available to the app now that a real set is in place.
  Brain as IconBrain,
  Command as IconCommand,
  Check as IconCheck,
  X as IconClose,
  ChevronRight as IconChevronRight,
  Copy as IconCopy,
  RefreshCw as IconRefresh,
  Upload as IconUpload,
  Download as IconDownload,
  Zap as IconZap,
  FileText as IconFile,
  Link2 as IconLink,
  PanelLeft as IconPanel,
} from 'lucide-react';
